import React from 'react';
import "./Text.css";

const Password = () => {
  return (
    <input
      type="password"
      id="password"
      name="password"
      placeholder="Enter OTP"
      className="password"
    ></input>
  );
};

export default Password;
